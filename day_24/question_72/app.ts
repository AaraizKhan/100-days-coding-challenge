{
    let blocklet = "My name is Aaraiz"
    const blockconst = "My name is khan"
    console.log(blocklet);
    console.log(blockconst); // we used let and const variable inside the block so it prints the name
}

    console.log(blocklet);   
    console.log(blockconst);// here the both names are not printed just because they 
    // have been used outside the block,which shows that the values or 
    // variable used outside the block has no importance.
